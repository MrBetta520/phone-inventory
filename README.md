# 手机仓库库存管理

一个可直接运行在 GitHub Pages 的手机仓库库存网页软件。

功能：快递单号 → IMEI → UPC 连续扫码、UPC 自动识别型号/颜色/容量、重复拦截、入库、出库、当前库存、统计、UPC 型号库、CSV 批量导入和 Excel/CSV 导出。

数据保存在当前电脑浏览器的 `localStorage` 中，不会上传到 GitHub。

主要源码：`index.html`、`styles.css`、`app.js`。原始 React/Sites 版本保存在 `app/` 目录中。
